
http_server({
  request = {
    reply = f f(request, data) {
      print(request.method, request.url)
      return 'your_data = ' + data
    };
    onerror = f f(error, request, response) {
      return "ERROR: " + error.message
    }
  };
  server = {
    oncreate = f f(options) {
      print("http server created using toylang... listening to port", options.server.port)
      return 0
    };
    onerror = f f(error) {
      print(error)
      return 0
    };
    port = 1234
  }
})
